Run the below command to install the dependencies
  pip install requirements.txt

Go to \Agentic\Resume_Screening, run below command to run the app
  uvicorn app.main:app --reload 

Go to \Agentic\Resume_Screening\app, run the below command to start streamlit UI
  streamlit run .\streamlit_app.py
